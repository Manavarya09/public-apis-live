{
  "name": "public-apis-live-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "refresh": "tsx scripts/refresh.ts",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
